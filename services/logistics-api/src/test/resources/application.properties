# The test profile of the suite. It is declared here, and not on every test class, so a
# test that forgets an annotation still runs against the in-memory database instead of
# trying to reach the PostgreSQL of the local stack.
spring.profiles.active=test

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.16</version>
    <relativePath/>
  </parent>

  <groupId>com.coobi.logistics</groupId>
  <artifactId>stream-processor</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <name>stream-processor</name>
  <description>Kafka Streams service that validates vehicle telemetry, keeps per-vehicle state, detects speeding and stopped vehicles and persists the derived state in PostgreSQL (MVP-2 to MVP-4).</description>

  <properties>
    <java.version>21</java.version>
  </properties>

  <dependencies>
    <!-- Web server only to expose the Actuator endpoints over HTTP. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <!--
      Micrometer registry that renders every meter - the logistics_* metrics of this service,
      the Kafka Streams binders and the JVM metrics - in the Prometheus exposition format
      (MVP-8.1 and MVP-8.2), on /actuator/prometheus.
    -->
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-registry-prometheus</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.kafka</groupId>
      <artifactId>spring-kafka</artifactId>
    </dependency>
    <!-- Managed by spring-boot-dependencies, so both versions stay in step. -->
    <dependency>
      <groupId>org.apache.kafka</groupId>
      <artifactId>kafka-streams</artifactId>
    </dependency>
    <!--
      Persistence layer (MVP-4). Plain JDBC rather than an ORM: the service writes two
      upserts per accepted record, and the schema is owned by Flyway, so there is no
      mapping layer to keep in step with the migrations.
    -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jdbc</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <!-- Flyway 10+ ships database support in separate modules. -->
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- TopologyTestDriver: runs the topology without a broker. -->
    <dependency>
      <groupId>org.apache.kafka</groupId>
      <artifactId>kafka-streams-test-utils</artifactId>
      <scope>test</scope>
    </dependency>
    <!--
      The persistence tests run the shipped Flyway migrations and the production SQL
      against an in-memory database in PostgreSQL mode, so the suite needs no server.
    -->
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!--
      MVP-9.2 and MVP-9.3: the container-backed integration tests.

      The profile is activated with `-Dintegration-tests` (or `-Pintegration-tests`) and does
      three things, all of them test-only:

      * it adds the Testcontainers modules the integration tests need, so the default build
        resolves no container dependency at all;
      * it adds `src/it/java` as a second test source root, so the integration tests are not
        even compiled by `mvn test` - a machine without Docker (or without the images) keeps
        running the unit suite exactly as before;
      * it extends the surefire includes with `**/*IT.java`, so the integration tests run in
        the same `test` phase while the unit tests keep running next to them.

      The images are the ones of `compose.yml` (apache/kafka:4.3.1 and postgres:18.6), so the
      tests exercise the versions the local stack runs. A machine without a Docker daemon
      skips the integration tests with the reason Testcontainers reports
      (`@Testcontainers(disabledWithoutDocker = true)`) instead of failing the build.
    -->
    <profile>
      <id>integration-tests</id>
      <activation>
        <property>
          <name>integration-tests</name>
        </property>
      </activation>
      <dependencies>
        <dependency>
          <groupId>org.testcontainers</groupId>
          <artifactId>testcontainers</artifactId>
          <scope>test</scope>
        </dependency>
        <dependency>
          <groupId>org.testcontainers</groupId>
          <artifactId>junit-jupiter</artifactId>
          <scope>test</scope>
        </dependency>
        <dependency>
          <groupId>org.testcontainers</groupId>
          <artifactId>kafka</artifactId>
          <scope>test</scope>
        </dependency>
        <dependency>
          <groupId>org.testcontainers</groupId>
          <artifactId>postgresql</artifactId>
          <scope>test</scope>
        </dependency>
      </dependencies>
      <build>
        <plugins>
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>build-helper-maven-plugin</artifactId>
            <version>3.6.1</version>
            <executions>
              <execution>
                <id>add-integration-test-sources</id>
                <phase>generate-test-sources</phase>
                <goals>
                  <goal>add-test-source</goal>
                </goals>
                <configuration>
                  <sources>
                    <source>${project.basedir}/src/it/java</source>
                  </sources>
                </configuration>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <configuration>
              <!--
                The default includes of surefire, plus `*IT`, which is how the integration
                tests of `src/it/java` are named. Declaring the list explicitly is required:
                a configured list replaces the defaults instead of extending them.
              -->
              <includes>
                <include>**/Test*.java</include>
                <include>**/*Test.java</include>
                <include>**/*Tests.java</include>
                <include>**/*TestCase.java</include>
                <include>**/*IT.java</include>
              </includes>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
